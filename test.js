//create new branch for each problem
//write tests first in test.js to make sure they fail
//APC the test.js file to prove we wrote them first!
//complete the first six questions in test.js
//write your coe as functions in app.js

//problem one
  it ('should test that 10 + 5 = 15', function () { assert(15 == app.sum(10,5)); });
  it ('should test that 8 + 16 = 24', function () { assert(24) == app.sum(8,16); });
  it ('should test that 33 + 21 = 54', function () { assert(54) == app.sum(33,21); });
